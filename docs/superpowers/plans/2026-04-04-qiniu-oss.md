# 七牛云 OSS 集成实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 增加七牛云 OSS 通用图片上传接口，支持草稿编辑器内粘贴/拖拽上传，并在发布到微信公众号时自动把草稿中的七牛云图片同步到微信 MP。

**Architecture:** 后端新增 `QiniuOSSClient`（封装官方 SDK）和 `POST /uploads/image` 路由；前端在草稿 MDEditor 增加图片粘贴/拖拽处理；发布草稿时在后端提取七牛域名图片，下载后调用微信 `upload_image` 替换 URL。

**Tech Stack:** FastAPI, Beanie, MongoDB, Qiniu Python SDK, React, TanStack Query, `@uiw/react-md-editor`

---

## 文件结构

| 文件 | 责任 |
|------|------|
| `backend/app/core/config.py` | 新增 `QINIU_*` 环境变量定义 |
| `backend/app/core/qiniu_oss.py` | `QiniuOSSClient` 异步上传封装 |
| `backend/app/api/routes/uploads.py` | `POST /uploads/image` 路由 |
| `backend/app/api/main.py` | 挂载 `uploads` 路由 |
| `backend/app/api/routes/drafts.py` | 发布流程中插入七牛图片同步逻辑 |
| `backend/app/core/markdown.py` | 复用 `extract_images_from_markdown()` |
| `frontend/src/routes/_layout/drafts/$id.tsx` | 编辑器增加粘贴/拖拽图片上传 |
| `tests/api/routes/test_uploads.py` | 上传接口测试（mock Qiniu） |
| `backend/pyproject.toml` | 新增 `qiniu` 依赖 |

---

### Task 1: 增加七牛云环境变量

**Files:**
- Modify: `backend/app/core/config.py`

- [ ] **Step 1: 在 `Settings` 类中添加 Qiniu 字段**

在 `backend/app/core/config.py` 的 `Settings` 类末尾（`FIRST_SUPERUSER_PASSWORD` 之后、`_check_default_secret` 之前）添加：

```python
    QINIU_ACCESS_KEY: str = ""
    QINIU_SECRET_KEY: str = ""
    QINIU_BUCKET: str = ""
    QINIU_DOMAIN: str = ""  # e.g. https://cdn.example.com
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/core/config.py
git commit -m "feat: add qiniu oss env config fields"
```

---

### Task 2: 创建 QiniuOSSClient

**Files:**
- Create: `backend/app/core/qiniu_oss.py`

- [ ] **Step 1: 安装 qiniu 依赖**

Run: `cd backend && uv add qiniu>=7.13.0`

- [ ] **Step 2: 创建 QiniuOSSClient**

在 `backend/app/core/qiniu_oss.py` 写入：

```python
"""Qiniu OSS client for async file uploads."""

import asyncio
from typing import Self

from qiniu import Auth, put_data

from app.core.config import settings


class QiniuOSError(Exception):
    """Exception raised for Qiniu OSS errors."""

    def __init__(self, message: str) -> None:
        super().__init__(message)


class QiniuOSSClient:
    """Async wrapper around Qiniu Python SDK."""

    def __init__(self, access_key: str, secret_key: str, bucket: str, domain: str) -> None:
        self.auth = Auth(access_key, secret_key)
        self.bucket = bucket
        self.domain = domain.rstrip("/")

    @classmethod
    def from_settings(cls) -> Self:
        """Create client from app settings."""
        if not settings.QINIU_ACCESS_KEY or not settings.QINIU_SECRET_KEY:
            raise QiniuOSError("Qiniu OSS credentials are not configured")
        if not settings.QINIU_BUCKET:
            raise QiniuOSError("Qiniu OSS bucket is not configured")
        if not settings.QINIU_DOMAIN:
            raise QiniuOSError("Qiniu OSS domain is not configured")
        return cls(
            access_key=settings.QINIU_ACCESS_KEY,
            secret_key=settings.QINIU_SECRET_KEY,
            bucket=settings.QINIU_BUCKET,
            domain=settings.QINIU_DOMAIN,
        )

    def _upload_sync(self, data: bytes, key: str) -> str:
        """Synchronous upload using put_data."""
        token = self.auth.upload_token(self.bucket, key)
        ret, info = put_data(token, key, data)
        if info.status_code != 200 or not ret:
            raise QiniuOSError(f"Qiniu upload failed: {info}")
        return f"{self.domain}/{key}"

    async def upload_file(self, data: bytes, filename: str) -> str:
        """Upload bytes to Qiniu OSS asynchronously.

        Args:
            data: Raw file bytes.
            filename: Object key (filename) in the bucket.

        Returns:
            str: Public URL of the uploaded file.
        """
        loop = asyncio.get_event_loop()
        return await loop.run_in_executor(None, self._upload_sync, data, filename)
```

- [ ] **Step 3: Commit**

```bash
git add backend/pyproject.toml backend/uv.lock backend/app/core/qiniu_oss.py
git commit -m "feat: add QiniuOSSClient wrapper"
```

---

### Task 3: 增加上传路由及 OpenAPI Schema

**Files:**
- Create: `backend/app/api/routes/uploads.py`
- Modify: `backend/app/api/main.py`

- [ ] **Step 1: 创建 uploads 路由**

在 `backend/app/api/routes/uploads.py` 写入：

```python
import uuid
from typing import Any

from fastapi import APIRouter, File, HTTPException, UploadFile, status

from app.api.deps import CurrentUser
from app.core.qiniu_oss import QiniuOSSClient, QiniuOSError

router = APIRouter(prefix="/uploads", tags=["uploads"])

ALLOWED_IMAGE_TYPES = {"image/jpeg", "image/png", "image/gif", "image/webp"}
MAX_FILE_SIZE = 5 * 1024 * 1024  # 5MB


@router.post("/image", response_model=dict[str, str])
async def upload_image(
    current_user: CurrentUser,  # noqa: ARG001
    file: UploadFile = File(...),
) -> Any:
    """Upload an image file to Qiniu OSS.

    Returns the public URL of the uploaded image.
    """
    if file.content_type not in ALLOWED_IMAGE_TYPES:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=f"Unsupported file type: {file.content_type}. Allowed: {', '.join(ALLOWED_IMAGE_TYPES)}",
        )

    data = await file.read()
    if len(data) > MAX_FILE_SIZE:
        raise HTTPException(
            status_code=status.HTTP_413_REQUEST_ENTITY_TOO_LARGE,
            detail=f"File size exceeds {MAX_FILE_SIZE // (1024 * 1024)}MB limit",
        )

    ext = file.filename.split(".")[-1] if "." in file.filename else "jpg"
    key = f"{uuid.uuid4().hex}.{ext}"

    try:
        client = QiniuOSSClient.from_settings()
        url = await client.upload_file(data, key)
    except QiniuOSError as exc:
        raise HTTPException(
            status_code=status.HTTP_503_SERVICE_UNAVAILABLE,
            detail=str(exc),
        ) from exc

    return {"url": url}
```

- [ ] **Step 2: 挂载 uploads 路由**

在 `backend/app/api/main.py` 中找到 `api_router` 的 `include_router` 列表，在 `skills` 之类的附近添加：

```python
from app.api.routes import uploads

api_router.include_router(uploads.router)
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/api/routes/uploads.py backend/app/api/main.py
git commit -m "feat: add POST /uploads/image endpoint"
```

---

### Task 4: 后端发布草稿时自动同步七牛图片到微信 MP

**Files:**
- Modify: `backend/app/api/routes/drafts.py`

- [ ] **Step 1: 阅读现有 drafts.py publish_draft 函数**

先读取 `backend/app/api/routes/drafts.py` 中 `publish_draft` 函数的完整代码，确认插入点。

- [ ] **Step 2: 导入依赖**

在 `backend/app/api/routes/drafts.py` 顶部增加导入：

```python
import httpx

from app.core.config import settings
from app.core.markdown import extract_images_from_markdown
from app.core.qiniu_oss import QiniuOSSClient
```

- [ ] **Step 3: 获取 markdown.py 的 extract_images_from_markdown**

先读取 `backend/app/core/markdown.py` 确认 `extract_images_from_markdown` 函数签名和返回值。如果不存在该函数，则需要创建它。

- [ ] **Step 4: 在 publish_draft 中插入图片同步逻辑**

在 `publish_draft` 中，找到把 `content` 传给 `markdown_to_wechat_html()` 之前的位置，插入以下逻辑：

```python
    # Sync Qiniu images to WeChat MP before publishing
    qiniu_domain = settings.QINIU_DOMAIN.rstrip("/")
    if qiniu_domain and draft.content:
        image_urls = extract_images_from_markdown(draft.content)
        qiniu_urls = [url for url in image_urls if url.startswith(qiniu_domain)]
        if qiniu_urls:
            async with httpx.AsyncClient(timeout=30.0) as http_client:
                for raw_url in qiniu_urls:
                    try:
                        response = await http_client.get(raw_url)
                        response.raise_for_status()
                        image_data = response.content
                        filename = raw_url.split("/")[-1] or "image.jpg"
                        mp_url = await wechat_client.upload_image(image_data, filename)
                        draft.content = draft.content.replace(raw_url, mp_url)
                    except Exception as exc:  # noqa: BLE001
                        import logging
                        logging.warning("Failed to sync Qiniu image %s to WeChat MP: %s", raw_url, exc)
```

**注意：** 需要确保 `draft` 对象被修改后的 `content` 用于后续 `html_content = markdown_to_wechat_html(draft.content)` 和 `wechat_client.add_draft(...)` 调用。

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/routes/drafts.py
git commit -m "feat: sync qiniu images to wechat mp on publish"
```

---

### Task 5: 前端草稿编辑器支持图片粘贴/拖拽上传

**Files:**
- Modify: `frontend/src/routes/_layout/drafts/$id.tsx`

- [ ] **Step 1: 添加上传 mutation**

在 `DraftEditorContent` 组件中，现有的 mutation 附近添加：

```typescript
  const uploadImageMutation = useMutation({
    mutationFn: async (file: File) => {
      const formData = new FormData()
      formData.append("file", file)
      const response = await fetch("/api/v1/uploads/image", {
        method: "POST",
        body: formData,
      })
      if (!response.ok) {
        const errorData = await response.json().catch(() => ({ detail: "上传失败" }))
        throw new Error(errorData.detail || "上传失败")
      }
      return response.json() as Promise<{ url: string }>
    },
    onError: () => showErrorToast("图片上传失败"),
  })
```

- [ ] **Step 2: 创建图片上传辅助函数**

在组件中添加处理函数（在 `handleContentChange` 附近）：

```typescript
  const insertImageToMarkdown = (url: string) => {
    const imageMarkdown = `\n![图片](${url})\n`
    setContent((prev) => prev + imageMarkdown)
    saveMutation.mutate({ content: content + imageMarkdown })
  }

  const handleImageUpload = async (file: File) => {
    if (!file.type.startsWith("image/")) return
    try {
      const result = await uploadImageMutation.mutateAsync(file)
      insertImageToMarkdown(result.url)
      showSuccessToast("图片上传成功")
    } catch {
      // error handled by mutation onError
    }
  }

  const handlePaste = (event: React.ClipboardEvent) => {
    const files = Array.from(event.clipboardData.files)
    const imageFile = files.find((f) => f.type.startsWith("image/"))
    if (imageFile) {
      event.preventDefault()
      handleImageUpload(imageFile)
    }
  }

  const handleDrop = (event: React.DragEvent) => {
    const files = Array.from(event.dataTransfer.files)
    const imageFile = files.find((f) => f.type.startsWith("image/"))
    if (imageFile) {
      event.preventDefault()
      handleDropUpload(imageFile)
    }
  }
```

**修正：** `handleDropUpload` 上面没有定义，请直接用 `handleImageUpload`：

```typescript
  const handleDrop = (event: React.DragEvent) => {
    const files = Array.from(event.dataTransfer.files)
    const imageFile = files.find((f) => f.type.startsWith("image/"))
    if (imageFile) {
      event.preventDefault()
      handleImageUpload(imageFile)
    }
  }
```

- [ ] **Step 3: 绑定事件到编辑器容器**

找到包裹 `MDEditor` 的 `div`，把 `handlePaste` 和 `handleDrop` 绑定上去：

```tsx
        <div
          className="flex-1 min-w-0"
          data-color-mode="auto"
          onContextMenu={handleContextMenu}
          onPaste={handlePaste}
          onDrop={handleDrop}
        >
          <MDEditor
            value={content}
            onChange={handleContentChange}
            height="100%"
            preview="live"
          />
        </div>
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/routes/_layout/drafts/\$id.tsx
git commit -m "feat: support paste/drop image upload in draft editor"
```

---

### Task 6: 生成前端 OpenAPI Client

**Files:**
- Regenerate: `frontend/src/client/`

- [ ] **Step 1: 生成 OpenAPI schema 并重新生成 client**

Run from repo root:

```bash
bash scripts/generate-client.sh
```

或分别执行：

```bash
cd backend && uv run python -m app.main --generate-openapi ../openapi.json
cd ../frontend && pnpm run generate-client
```

（根据项目实际脚本选择）

- [ ] **Step 2: Commit**

```bash
git add openapi.json frontend/src/client/
git commit -m "chore: regenerate openapi client for uploads endpoint"
```

---

### Task 7: 后端上传路由测试

**Files:**
- Create: `tests/api/routes/test_uploads.py`

- [ ] **Step 1: 编写 test upload image with mocked qiniu**

在 `tests/api/routes/test_uploads.py` 写入：

```python
from unittest.mock import patch

import pytest
from httpx import AsyncClient

from app.main import app


@patch("app.api.routes.uploads.QiniuOSSClient.from_settings")
@patch("app.api.routes.uploads.QiniuOSSClient.upload_file")
async def test_upload_image_success(mock_upload, mock_from_settings, client: AsyncClient) -> None:
    mock_from_settings.return_value = mock_from_settings
    mock_upload.return_value = "https://cdn.example.com/test.jpg"

    # Use an in-memory image file
    data = b"fake-image-data"
    response = await client.post(
        "/api/v1/uploads/image",
        files={"file": ("test.jpg", data, "image/jpeg")},
    )
    assert response.status_code == 200
    assert response.json()["url"] == "https://cdn.example.com/test.jpg"


async def test_upload_image_unsupported_type(client: AsyncClient) -> None:
    response = await client.post(
        "/api/v1/uploads/image",
        files={"file": ("test.txt", b"not-an-image", "text/plain")},
    )
    assert response.status_code == 400


async def test_upload_image_too_large(client: AsyncClient) -> None:
    big_data = b"x" * (6 * 1024 * 1024)
    response = await client.post(
        "/api/v1/uploads/image",
        files={"file": ("big.jpg", big_data, "image/jpeg")},
    )
    assert response.status_code == 413
```

**注意：** 如果 `client` fixture 不是 `AsyncClient` 而是其他类型，请根据项目 `tests/conftest.py` 的实际 fixture 名称和类型调整。

- [ ] **Step 2: 运行测试**

Run: `cd backend && uv run pytest tests/api/routes/test_uploads.py -v`

Expected: 3 tests PASS（需 MongoDB 已启动，否则测试集器级 setup 会报错）

- [ ] **Step 3: Commit**

```bash
git add tests/api/routes/test_uploads.py
git commit -m "test: add upload image route tests"
```

---

### Task 8: 运行 Lint / Type Check

**Files:**
- 所有修改过的文件

- [ ] **Step 1: 后端 lint**

Run: `cd backend && bash scripts/lint.sh`

- [ ] **Step 2: 前端 lint / type-check**

Run: `cd frontend && pnpm run lint && pnpm run build`（`build` 同时包含 tsc）

- [ ] **Step 3: 修复任何问题**

根据 lint / build 输出修复类型错误或格式化问题。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "style: fix lint and type errors"
```

---

### Task 9: Docker 运行验证

- [ ] **Step 1: 启动完整 dev stack**

```bash
docker compose up -d
```

- [ ] **Step 2: 验证服务状态**

```bash
docker compose ps | grep -E "(backend|frontend|celery)"
curl -s http://localhost:8000/api/v1/utils/health-check/
curl -s http://localhost:5173/ > /dev/null && echo "Frontend OK"
```

- [ ] **Step 3: 手动测试上传接口**

```bash
curl -X POST http://localhost:8000/api/v1/uploads/image \
  -F "file=@/path/to/test.jpg"
```

（可选，需要配置真实 Qiniu 凭证；未配置时应返回 503）

- [ ] **Step 4: Commit 任何修正**

如果有 Dockerfile / docker-compose 相关修正，一起提交。

---

## Spec 自检

- [x] 后端上传接口：Task 3
- [x] 前端编辑器图片粘贴/拖拽：Task 5
- [x] 发布时自动同步七牛图片到微信 MP：Task 4
- [x] 环境变量配置：Task 1
- [x] QiniuOSSClient 封装：Task 2
- [x] 前端 client 重新生成：Task 6
- [x] 测试覆盖：Task 7
- [x] 部署验证：Task 9
